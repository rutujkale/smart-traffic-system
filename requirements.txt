ultralytics>=8.2.0
opencv-python>=4.9.0
flask>=3.0.0
numpy>=1.26.0
dimod>=0.12.0
dwave-neal>=0.6.0
# Optional — only needed to run signal_optimizer.solve_on_quantum_hardware
# on real D-Wave quantum annealing hardware instead of the classical
# simulated-annealing sampler used by default:
# dwave-system>=1.20.0
